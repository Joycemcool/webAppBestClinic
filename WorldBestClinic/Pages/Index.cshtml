@page
@model IndexModel
@{
    ViewData["Title"] = "Home";
}

<div class="text-center">
    <h1 class="display-4">Welcome to the world best clinic</h1>
    <p>Learn about <a href="https://regencyparkfamilypractice.ca/">Regencypark Family Practice</a>.</p>
</div>

<div class="container">
    <div class="row">
        @foreach (var item in Model.Services)
        {
            string imagePath = "/ServicePhotos/" + item.FileName;
            <div class="col mb-4">
                <div class="card" style="width: 18rem;">
                    <img src="@imagePath" class="card-img-top" style="width: 100%; height: 200px; alt="ServicePhoto">
                    <div class="card-body">
                        <p class="card-text">Check our services.</p>
                    </div>
                </div>
            </div>
        }

    </div>

   @*  @foreach (var item in Model.Services)
    {
        string imagePath = "/ServicePhotos/" + item.FileName;

        <div class="pin-box">
            <img src="@imagePath" class="mb-2" data-bs-toggle="modal" data-bs-target="#photoModal">
            @*<img src="@imagePath" class="mb-2" data-bs-toggle="modal" data-bs-target="#photoModal" data-bs-photo="@item.PhotoId">
        </div>
    }
 *@
</div>
